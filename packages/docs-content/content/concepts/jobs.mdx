---
title: Jobs
description: The core scheduling unit in BackupOS — a source, a repository, a schedule, and optional hooks.
---

# Jobs

A job combines a **source** (what to back up), a **repository** (where to store it), a **schedule** (when to run), and optional **hooks** (what to do before and after).

## Lifecycle

```
idle → queued → running → success
                        ↘ failed
                        ↘ cancelled
```

A job is **idle** between scheduled runs. The scheduler **queues** it at the configured cron time. The assigned agent picks it up and moves it to **running**. On completion: **success**, **failed**, or **cancelled** (if manually stopped).

## Run phases

Each run moves through five ordered phases:

<JobPhases />

<Tip>Enable verification on at least your most critical jobs. A job that completes the backup but fails verification is a sign your repository is degraded before you actually lose data.</Tip>

## Coverage entries

A job can optionally be linked to a **coverage entry** — an Infra OS service registry record. This lets the dashboard show which services are protected by which jobs, and surfaces unprotected services that need a backup setup.

The link is optional and is set on the new-job form via the **Link to coverage entry** dropdown.
